#!/usr/bin/env bash
#
# This script is used to install bash plugins and packages if necessary.
# Use this script as a callee of "autoload.sh" of the pipeline.
#

#update-alternatives --skip-auto --config x-terminal-emulator $OUTPUT
#update-alternatives --skip-auto --config --set x-terminal-emulator /usr/bin/xterm
